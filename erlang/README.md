### Erlang client

This is the client that sends requests to the workers in some other language.

Currently uses rebar2.

#### Running

```
rebar get-deps
rebar compile
rebar escriptize
./erlang_server
```

